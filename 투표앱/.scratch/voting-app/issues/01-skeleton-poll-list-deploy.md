# 01: 뼈대 — 투표 목록, 헤더, 첫 Vercel 배포

**만들 것:** 사이트(로컬과 Vercel)를 열면 모든 페이지 헤더에 "만든 사람: 안한석"이 보이고, 첫 화면에 Neon에 저장된 투표 목록이 보인다. 진행 중인 투표가 먼저(마감이 가까운 순), 마감된 투표가 나중에(최근 마감 순) 나오고, 각 투표에 "마감까지 N시간 남음" 또는 "마감됨"이 표시된다. 투표가 없으면 안내 문구가 보인다. 샘플 투표는 시드 스크립트로 넣는다. 투표 도메인 모듈과 Vitest도 여기서 준비한다.

**먼저 끝나야 할 티켓:** 없음 (바로 시작 가능)

**Status:** done

- [x] 공통 레이아웃 헤더에 "만든 사람: 안한석" 표시, 휴대폰에서도 잘 보임
- [x] 투표(마감 시간은 `timestamptz`)와 선택지 테이블을 다시 실행해도 안전한 스크립트로 생성
- [x] 시드 스크립트로 진행 중·마감된 샘플 투표 몇 개 입력
- [x] 첫 화면에 위 순서대로 목록 표시, 남은 시간/마감됨은 서버 시각 기준, KST로 계산
- [x] 투표가 없을 때 안내 문구
- [x] 투표 도메인 모듈이 일반 데이터와 명시적인 "현재 시각"으로 진행 중/마감됨, 정렬, 표시 문구를 판단
- [x] Vitest 설치와 `npm test` 스크립트. 마감 시간 정각·1초 전·1초 후, 정렬, 표시 문구 테스트
- [x] Vercel 프로젝트 설정에 `DATABASE_URL`을 넣고 배포. 실제 URL에서 시드한 목록이 보임
- [x] 프레임워크 코드를 쓰기 전에 `node_modules/next/dist/docs/`의 Next.js 문서 확인(이 버전은 바뀐 점이 많음)
